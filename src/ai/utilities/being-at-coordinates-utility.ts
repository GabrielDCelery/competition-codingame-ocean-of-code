import { ISubmarine } from '../../submarines';
import { ICoordinates } from '../../maps';

export const calculateBeingAtCoordinatesUtility = ({
  coordinates,
  submarine,
  opposingSubmarines,
}: {
  coordinates: ICoordinates;
  submarine: ISubmarine;
  opposingSubmarines: ISubmarine[];
}): number => {};
